Team Review: Test Coverage & Code Quality

  ---
  Agreements (all 3 agents aligned)

  1. Draft entity review is the biggest functional gap
  E2E: 10+ tests skipped, blocked by Tauri mock. Reviewer: no integration test catches draft→DB→response flow. Refactor-cleaner: draft seeding infrastructure missing
  from tauri-mock.js.

  2. .toBeTruthy() on RTL queries is widespread and meaningless
  Reviewer flagged 35+ occurrences in DocumentList.test.tsx and UploadDialog tests. getByText() throws on miss — the assertion can never detect a real failure. Entire
   test suite has false confidence.

  3. Oversized files violate project rules and hurt maintainability
  Refactor-cleaner: UploadDialog.tsx (1008 lines), SettingsPage (1002 lines), DocumentDetailClient.tsx (994 lines), documents_search_filtered in Rust (1432 lines).
  All exceed the 800-line max.

  4. No cross-module integration tests for extraction pipeline
  Reviewer: 0 end-to-end tests from OCR text → contact/clinic/notes extraction → IPC response. E2E-runner: clinical notes and role-label extraction (Phases 108–109)
  have zero tests at any layer.

  5. Recurrence & reminders flows completely untested
  E2E: zero tests for recurring appointments or reminder scheduling. Reviewer: no Rust unit tests for recurrence IPC commands.

  ---
  Conflicts (agents disagreed)

  Settings page coverage: E2E-runner called it "CRITICAL GAP (zero E2E)". Refactor-cleaner focused on console.error statements but flagged the 1002-line size as the
  real problem. Resolution: the console.error calls are a noise issue; the zero E2E coverage is the risk. Fix the selectors, add 3 settings E2E tests.

  Selector brittleness priority: E2E-runner wants data-testid everywhere. Reviewer wants assertions strengthened first (.toBeTruthy() → .toBeInTheDocument()).
  Resolution: fix .toBeTruthy() first (higher false-confidence risk), then sweep for missing data-testid.

  ---
  Consensus: Prioritized Action List

  P0 — Fix before next release (blocks trust)
  1. Replace all .toBeTruthy() on RTL queries with .toBeInTheDocument() or specific value assertions — DocumentList.test.tsx, UploadDialog.test.tsx,
  DoctorSuggestionBanner.test.tsx (~35 occurrences)
  2. Add beforeEach sessionStorage reset to all E2E specs (state bleed between tests)
  3. Replace 8 waitForTimeout calls with condition-based waits

  P1 — Add before v1.5 ship
  4. Add recurrence + reminders E2E (skeletons above, ~5 tests)
  5. Add clinical notes extraction tests once Phase 109 lands (3 E2E + Rust unit tests)
  6. Add ClinicSuggestionBanner.test.tsx (missing entirely)
  7. Add 1 extraction integration test: real PDF → ExtractionSuggestions struct fully populated

  P2 — Refactor (debt, not risk)
  8. Split UploadDialog.tsx into useUploadQueue hook + sub-components
  9. Split SettingsPage into section sub-components
  10. Extract shared SuggestionBanner base component (ApptSuggestionBanner, DoctorSuggestionBanner, ClinicSuggestionBanner share 80% markup)
  11. Remove export from FileQueueStatus, FileQueueItem, ExtractedAddress (unused outside file)
  12. Standardize ApptSuggestionBanner colors to CSS variables (hardcoded Tailwind blues)

  P3 — Nice to have
  13. Add visual regression screenshots for timeline + upload dialog
  14. Add axe-core accessibility assertions to critical flows
  15. Add cross-module integration test: documents.rs command → SQLite → response shape

  ---
  Want me to start on any of these? Highest ROI is P0 item 1 — fixing .toBeTruthy() across the test suite takes ~30 min and immediately improves test reliability
  signal.
